# QA Fixes Implementation Plan

**Goal:** Fix all critical, major, and minor bugs identified by QA audit.

**Execution:** 3 parallel agents on independent file groups.

---

## Agent A — Frontend fixes (index.html)

All fixes in a single file, executed sequentially by one agent.

### A1. CRITICAL: Fix "Voir les X suivantes" button (F-6)
- **Line ~1441**: `prospListLimit = 200` at start of `renderProspectList()` overwrites the value set by click handler
- **Fix**: Move initialization outside function, or use a parameter/flag

### A2. MAJOR: Close detail panel on mode switch (F-17)
- **Line ~1192**: `switchMode()` doesn't close the detail panel
- **Fix**: Add detail panel close in `switchMode()`

### A3. MAJOR: Bottom bar ignores activeFilter in surveillance/securite (F-3)
- **Lines ~2436-2458**: `updateBottomBar()` iterates all communes without checking `activeFilter`
- **Fix**: Filter by activeFilter when computing stats in surveillance/securite bottom bar

### A4. MAJOR: Search index only includes maires.json communes (F-8)
- **Lines ~2487-2490**: Search index built from `for (var code in maires)` only
- **Fix**: Also index communes from surv/prosp/delinq that aren't in maires

### A5. MINOR: Detail panel shows "0 /10k" instead of "pas de données" (F-10)
- **Lines ~825-841**: Section surveillance shown when `s || p` but sData can be empty
- **Fix**: Show "Données indisponibles" when sData is empty but section is shown due to prosp

### A6. MINOR: Tooltip/detail shows "undefined" for missing lb/nu (F-13/14)
- **Lines ~663, ~726-732**: Concatenation without checking if fields exist
- **Fix**: Fallback to empty string for missing lb/nu

### A7. MINOR: QPV badge inconsistent positioning (F-9)
- **Lines ~935-951**: Badge appended to `content.lastChild` regardless of which section is last
- **Fix**: Create dedicated section or append to header

### A8. UX: Default mode should be prospection
- **Lines ~183, ~298**: HTML button and JS variable default to prospection (or politique?)
- **Fix**: Ensure default is prospection

### A9. MINOR: Debounce population slider (T-1.3)
- **Line ~1323**: Slider triggers setStyle on every pixel movement
- **Fix**: Apply same debounce pattern as scheduleProspRefresh

### A10. MINOR: Add SRI hashes to CDN scripts (T-3.3)
- **Lines ~7, ~222-223**: unpkg.com scripts without integrity attribute
- **Fix**: Add integrity and crossorigin attributes

---

## Agent B — Update Filosofi data (process_enrichment.py)

### B1. CRITICAL: Replace Filosofi 2013 with recent data (D-1)
- Research newer Filosofi data on data.gouv.fr (2021+ available)
- Update REVENUS_URL in process_enrichment.py
- Update column parsing if needed (new format may differ)
- Update METHODOLOGIE.md year references
- Update CLAUDE.md if needed

---

## Agent C — Research & cleanup

### C1. Research better stationnement payant source (D-2)
- Search data.gouv.fr for BNLS, stationnement payant, Cerema
- Document findings in this plan file
- If found: note URL and format for future implementation

### C2. Remove unused winners.json (T-5.2)
- Delete winners.json from repo

---

## Execution Order
All 3 agents run in parallel (no file dependencies between groups).
